   {% extends "wiki_base.html" %}
   {% block content %}
   {% for content in subjects %}
      <div class="content">
       <a href="{{paths}}?v={{content.key().id()}}" name="v" class="content-subject">{{"View this version"}}</a>
       <pre class="content-body">{{content.content}}</pre>

       <div class="content-body">edited by <b>{{content.username}}</b> at {{content.created}}</div>
   </div>

   {% endfor %}
   {% endblock %}
